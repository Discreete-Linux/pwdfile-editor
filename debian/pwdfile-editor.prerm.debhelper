
# Automatically added by dhpython:
if which pyclean >/dev/null 2>&1; then
	pyclean -p pwdfile-editor 
else
	dpkg -L pwdfile-editor | grep \.py$ | while read file
	do
		rm -f "${file}"[co] >/dev/null
  	done
fi

# End automatically added section
